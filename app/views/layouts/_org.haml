= unless current_organization.nil?
  %button{:class=>"linkbutton grid_4",
  :onClick=>"location.href='#{organizations_path}/#{current_organization.name}';return false"} #{current_organization.displayName}

